name: texfi_files
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.4.0+23

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Player
  media_kit: ^1.1.11
  media_kit_video: ^1.2.5
  media_kit_libs_video: ^1.0.5
  audio_service: ^0.18.15

  # Storage & platform
  path_provider: ^2.1.4
  shared_preferences: ^2.3.2
  file_picker: ^8.1.2
  open_filex: ^4.5.0

  # Networking helpers
  network_info_plus: ^6.1.1
  device_info_plus: ^11.1.0
  http: ^1.2.2
  mime: ^2.0.0
  intl: ^0.20.2
  uuid: ^4.5.1

  # UI / кастомизация
  flutter_colorpicker: ^1.1.0
  image_picker: ^1.1.2
  audio_metadata_reader: ^1.4.0
  url_launcher: ^6.3.1

  # Видео-превью (Android/iOS) и фоновый сервис (Android)
  get_thumbnail_video: ^0.7.3
  flutter_foreground_task: ^8.17.0

  # Голосовые сообщения и «поделиться»
  record: ^6.1.1
  share_plus: ^10.1.4

  # Безопасность: шифрование, PIN
  cryptography: ^2.7.0
  crypto: ^3.0.6
  local_auth: ^2.3.0

  # Кросс-девайс: QR-пары
  qr_flutter: ^4.1.0
  mobile_scanner: ^6.0.10

  # Явный запрос системных разрешений (камера для QR, уведомления для
  # медиа-шторки) — надёжнее, чем внутренние запросы отдельных плагинов.
  permission_handler: ^11.3.1

  # Desktop drag & drop
  desktop_drop: ^0.5.0

  # Приём «Поделиться в TexFi files» из других приложений
  receive_sharing_intent: ^1.8.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.14.1
  flutter_lints: ^6.0.0

flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/icon_1024.png"
  adaptive_icon_background: "#0B0B0E"
  adaptive_icon_foreground: "assets/icon_1024.png"
  remove_alpha_ios: false
  windows:
    generate: true
    image_path: "assets/icon_1024.png"
    icon_size: 256


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/icon_1024.png
    - assets/icon_512.png
    - assets/brand/

  fonts:
    # Пиксельный — только заголовки, метки кнопок и крупные числа.
    - family: PressStart2P
      fonts:
        - asset: assets/fonts/PressStart2P-Regular.ttf
    # Гротеск — весь читаемый текст. Лежит ассетом, а не тянется
    # google_fonts: приложение работает в локальной сети без интернета.
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter-Regular.ttf
          weight: 400
        - asset: assets/fonts/Inter-Medium.ttf
          weight: 500
        - asset: assets/fonts/Inter-SemiBold.ttf
          weight: 600

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
